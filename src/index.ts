/**
 * Public entry point for the `file-next` library.
 *
 * The library exposes:
 * - A storage adapter abstraction (S3 / R2 / in-memory for tests)
 * - React UI components built on shadcn/ui for upload, list, and delete
 *
 * Re-exports are added here as the API stabilizes.
 */

export {};
